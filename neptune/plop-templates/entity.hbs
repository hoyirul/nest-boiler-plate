/*
 * Copyright (c) 2026 Madhai
 * src/modules/v1/{{name}}/domains/{{name}}.entity.ts
 */

export class {{upper name}}Entity {
  id!: number;
  {{#each fieldsArray}}
  {{this.name}}!: {{this.tsType}};
  {{/each}}
  created_at!: Date;
  updated_at!: Date;
  deleted_at?: Date | null;

  constructor(data: Partial<{{upper name}}Entity>) {
    Object.assign(this, data);
  }
}
